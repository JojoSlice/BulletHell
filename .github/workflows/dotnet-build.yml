name: Build on PR

on:
  pull_request:
    branches:
      - main
      - develop

permissions:
  contents: read
  pull-requests: write
  checks: write
  actions: write

jobs:
  build:
    name: Build on Ubuntu
    runs-on: ubuntu-latest
    outputs:
      changed_files: ${{ steps.set_outputs.outputs.changed_files }}
      changed_lines: ${{ steps.set_outputs.outputs.changed_lines }}
      has_vulnerabilities: ${{ steps.set_outputs.outputs.has_vulnerabilities }}
      build_outcome: ${{ steps.set_outputs.outputs.build_outcome }}
      restore_outcome: ${{ steps.set_outputs.outputs.restore_outcome }}
      run_id: ${{ steps.set_outputs.outputs.run_id }}
      repository: ${{ steps.set_outputs.outputs.repository }}
      server_url: ${{ steps.set_outputs.outputs.server_url }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Count changed files and lines
        id: stats
        shell: bash
        run: |
          git fetch origin ${{ github.base_ref }}

          CHANGED_FILES=$(git diff --name-only origin/${{ github.base_ref }}...HEAD | { grep '\.cs$' || true; } | wc -l)
          echo "changed_files=${CHANGED_FILES}" >> "${GITHUB_OUTPUT}"

          CHANGED_LINES=$(git diff --numstat origin/${{ github.base_ref }}...HEAD | { grep '\.cs$' || true; } | awk '{sum+=$1+$2} END {print sum+0}')
          echo "changed_lines=${CHANGED_LINES:-0}" >> "${GITHUB_OUTPUT}"

      - name: Show stats
        shell: bash
        run: |
          echo "📊 Statistik för C# filer:"
          echo "================================"
          echo "Ändrade filer: ${{ steps.stats.outputs.changed_files }}"
          echo "Ändrade rader: ${{ steps.stats.outputs.changed_lines }}"
          echo "================================"

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: "9.0.x"

      - name: Install MonoGame dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libopenal-dev \
            libgl1-mesa-dev \
            libglu1-mesa-dev \
            xvfb

      - name: Cache NuGet packages
        uses: actions/cache@v5
        with:
          path: ~/.nuget/packages
          key: ubuntu-nuget-${{ hashFiles('**/*.csproj', '**/packages.lock.json') }}
          restore-keys: |
            ubuntu-nuget-

      - name: Restore dependencies
        id: restore
        run: dotnet restore BulletHellSpaceShooter.sln

      - name: Check for vulnerable packages (collect report, don't fail job)
        id: vulnerability-check
        shell: bash
        run: |
          echo "Checking for vulnerable NuGet packages..."
          # Collect the report even if empty
          dotnet list package --vulnerable --include-transitive 2>&1 | tee vulnerability-report.txt || true

          if grep -q "has the following vulnerable packages" vulnerability-report.txt; then
            echo "has_vulnerabilities=true" >> $GITHUB_OUTPUT
            echo "⚠️ Sårbara paket hittades!"
          else
            echo "has_vulnerabilities=false" >> $GITHUB_OUTPUT
            echo "✅ Inga sårbara paket hittades"
          fi

      - name: Build with Code Analysis
        id: dotnet_build
        run: dotnet build BulletHellSpaceShooter.sln --configuration Release --no-restore /p:TreatWarningsAsErrors=true

      - name: Build results
        if: success()
        shell: bash
        run: |
          echo "✅ Build lyckades på Ubuntu!"
          echo "Ändrade ${{ steps.stats.outputs.changed_files }} filer med ${{ steps.stats.outputs.changed_lines }} rader"

      # Upload vulnerability report so the comment job can include/truncate it
      - name: Upload vulnerability report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: vulnerability-report
          path: vulnerability-report.txt

      # Ensure job outputs are set even if earlier steps failed
      - name: Set job outputs
        id: set_outputs
        if: always()
        shell: bash
        run: |
          # Use expanded expressions to capture values
          echo "changed_files=${{ steps.stats.outputs.changed_files }}" >> "$GITHUB_OUTPUT"
          echo "changed_lines=${{ steps.stats.outputs.changed_lines }}" >> "$GITHUB_OUTPUT"
          echo "has_vulnerabilities=${{ steps.vulnerability-check.outputs.has_vulnerabilities }}" >> "$GITHUB_OUTPUT"
          echo "build_outcome=${{ steps.dotnet_build.outcome }}" >> "$GITHUB_OUTPUT"
          echo "restore_outcome=${{ steps.restore.outcome }}" >> "$GITHUB_OUTPUT"
          echo "run_id=${{ github.run_id }}" >> "$GITHUB_OUTPUT"
          echo "repository=${{ github.repository }}" >> "$GITHUB_OUTPUT"
          echo "server_url=${{ github.server_url }}" >> "$GITHUB_OUTPUT"

  publish-comment:
    name: Publish sticky PR comment
    needs: build
    if: always()
    runs-on: ubuntu-latest
    steps:
      - name: Download vulnerability report artifact (if present)
        uses: actions/download-artifact@v4
        with:
          name: vulnerability-report
          path: ./artifacts
        continue-on-error: true

      - name: Generate sticky comment body (includes vulnerability report)
        if: always()
        shell: bash
        run: |
          BUILD_OUTCOME="${{ needs.build.outputs.build_outcome }}"
          RESTORE_OUTCOME="${{ needs.build.outputs.restore_outcome }}"
          VULN="${{ needs.build.outputs.has_vulnerabilities }}"
          CHANGED_FILES="${{ needs.build.outputs.changed_files }}"
          CHANGED_LINES="${{ needs.build.outputs.changed_lines }}"
          RUN_ID="${{ needs.build.outputs.run_id }}"
          REPO="${{ needs.build.outputs.repository }}"
          SERVER_URL="${{ needs.build.outputs.server_url }}"

          CHANGED_FILES="${CHANGED_FILES:-0}"
          CHANGED_LINES="${CHANGED_LINES:-0}"

          failedStep='Okänt steg'
          errorDetails=''

          if [ "${RESTORE_OUTCOME}" = "failure" ]; then
            failedStep='Restore dependencies'
            errorDetails='Det gick inte att återställa NuGet-paket. Kontrollera att alla paketversioner är giltiga.'
          elif [ "${BUILD_OUTCOME}" = "failure" ]; then
            failedStep='Build with Code Analysis'
            errorDetails='Build misslyckades. Detta kan bero på:\n- Kompileringsfel i koden\n- Code analysis varningar (behandlas som fel)\n- Syntaxfel'
          fi

          if [ "${BUILD_OUTCOME}" = "success" ]; then
            status_line='✅ Build lyckades på Ubuntu!'
          else
            status_line='❌ Build misslyckades'
          fi

          if [ "${VULN}" = "true" ]; then
            vuln_summary='⚠️ Sårbara NuGet-paket hittades — fullständig lista nedan.'
          else
            vuln_summary='✅ Inga sårbara NuGet-paket hittades'
          fi

          VULN_REPORT_FILE="./artifacts/vulnerability-report.txt"
          VULN_CONTENT=""
          TRUNCATED_NOTICE=""

          if [ -f "${VULN_REPORT_FILE}" ]; then
            VULN_CONTENT=$(head -c 12000 "${VULN_REPORT_FILE}" || true)
            TOTAL_SIZE=$(wc -c < "${VULN_REPORT_FILE}" || echo 0)
            RETAINED_SIZE=$(printf "%s" "$VULN_CONTENT" | wc -c)
            if [ "${TOTAL_SIZE}" -gt "${RETAINED_SIZE}" ]; then
              TRUNCATED_NOTICE="_(Rapport förkortad — se artifact eller körningsloggar för hela innehållet)_"
            fi
          else
            VULN_CONTENT="Inga data (vulnerability-report.txt saknas)."
          fi

          {
            echo "## 📊 Build Statistik"
            echo
            echo "${status_line}"
            echo
            echo "### Ändringar i denna PR:"
            echo "- **Ändrade .cs filer:** ${CHANGED_FILES}"
            echo "- **Ändrade rader:** ${CHANGED_LINES}"
            echo
            echo "${vuln_summary}"
            echo

            if [ "${VULN}" = "true" ] && [ -n "${VULN_CONTENT}" ]; then
              echo "### Sårbara paket (utdrag)"
              echo '```'
              printf '%s\n' "${VULN_CONTENT}"
              echo '```'
              echo
              echo "${TRUNCATED_NOTICE}"
              echo
            fi

            if [ "${BUILD_OUTCOME}" != "success" ]; then
              echo "**Steg som failade:** ${failedStep}"
              echo
              echo "**Detaljer:** ${errorDetails}"
              echo
              echo "[Se fullständiga loggar här](${SERVER_URL}/${REPO}/actions/runs/${RUN_ID})"
            fi
          } > comment.md

      - name: Create or update sticky PR comment
        uses: marocchino/sticky-pull-request-comment@v2.9.0
        with:
          header: build
          path: comment.md

  run-tests:
    needs: build
    uses: ./.github/workflows/test-on-build.yml
    with:
      pr_number: ${{ github.event.pull_request.number }}
      commit_sha: ${{ github.sha }}
