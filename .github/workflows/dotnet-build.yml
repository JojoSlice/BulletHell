name: Build on PR

on:
  pull_request:
    branches:
      - main
      - develop

permissions:
  contents: read
  pull-requests: write
  checks: write
  actions: write

jobs:
  build:
    name: Build on Ubuntu
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Count changed files and lines
        id: stats
        shell: bash
        run: |
          git fetch origin ${{ github.base_ref }}

          CHANGED_FILES=$(git diff --name-only origin/${{ github.base_ref }}...HEAD | { grep '\.cs$' || true; } | wc -l)
          echo "changed_files=${CHANGED_FILES}" >> "${GITHUB_OUTPUT}"

          CHANGED_LINES=$(git diff --numstat origin/${{ github.base_ref }}...HEAD | { grep '\.cs$' || true; } | awk '{sum+=$1+$2} END {print sum+0}')
          echo "changed_lines=${CHANGED_LINES:-0}" >> "${GITHUB_OUTPUT}"

      - name: Show stats
        shell: bash
        run: |
          echo "📊 Statistik för C# filer:"
          echo "================================"
          echo "Ändrade filer: ${{ steps.stats.outputs.changed_files }}"
          echo "Ändrade rader: ${{ steps.stats.outputs.changed_lines }}"
          echo "================================"

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: "9.0.x"

      - name: Install MonoGame dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libopenal-dev \
            libgl1-mesa-dev \
            libglu1-mesa-dev \
            xvfb

      - name: Cache NuGet packages
        uses: actions/cache@v4
        with:
          path: ~/.nuget/packages
          key: ubuntu-nuget-${{ hashFiles('**/*.csproj', '**/packages.lock.json') }}
          restore-keys: |
            ubuntu-nuget-

      - name: Restore dependencies
        id: restore
        run: dotnet restore BulletHellSpaceShooter.sln

      - name: Check for vulnerable packages
        id: vulnerability-check
        continue-on-error: false
        run: |
          echo "Checking for vulnerable NuGet packages..."
          dotnet list package --vulnerable --include-transitive 2>&1 | tee vulnerability-report.txt

          if grep -q "has the following vulnerable packages" vulnerability-report.txt; then
            echo "has_vulnerabilities=true" >> $GITHUB_OUTPUT
            echo "⚠️ Sårbara paket hittades!"
            exit 1
          else
            echo "has_vulnerabilities=false" >> $GITHUB_OUTPUT
            echo "✅ Inga sårbara paket hittades"
          fi

      - name: Comment on vulnerabilities
        if: steps.vulnerability-check.outputs.has_vulnerabilities == 'true'
        uses: actions/github-script@v8
        with:
          script: |
            const fs = require('fs');
            const vulnReport = fs.readFileSync('vulnerability-report.txt', 'utf8');

            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `## ⚠️ Sårbara NuGet-paket hittades\n\n` +
                    `Följande sårbara paket upptäcktes i projektet:\n\n` +
                    `\`\`\`\n${vulnReport}\n\`\`\`\n\n` +
                    `Vänligen uppdatera dessa paket till säkra versioner.`
            });

      - name: Build with Code Analysis
        id: build
        run: dotnet build BulletHellSpaceShooter.sln --configuration Release --no-restore /p:TreatWarningsAsErrors=true

      - name: Build results
        if: success()
        shell: bash
        run: |
          echo "✅ Build lyckades på Ubuntu!"
          echo "Ändrade ${{ steps.stats.outputs.changed_files }} filer med ${{ steps.stats.outputs.changed_lines }} rader"

      - name: Comment PR on failure
        if: failure() && steps.vulnerability-check.outputs.has_vulnerabilities != 'true'
        uses: actions/github-script@v8
        with:
          script: |
            let failedStep = 'Okänt steg';
            let errorDetails = '';

            if ('${{ steps.restore.outcome }}' === 'failure') {
              failedStep = 'Restore dependencies';
              errorDetails = 'Det gick inte att återställa NuGet-paket. Kontrollera att alla paketversioner är giltiga.';
            } else if ('${{ steps.build.outcome }}' === 'failure') {
              failedStep = 'Build with Code Analysis';
              errorDetails = 'Build misslyckades. Detta kan bero på:\n- Kompileringsfel i koden\n- Code analysis varningar (behandlas som fel)\n- Syntaxfel';
            }

            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `## ❌ Build Misslyckades\n\n` +
                    `**Steg som failade:** ${failedStep}\n\n` +
                    `**Detaljer:** ${errorDetails}\n\n` +
                    `[Se fullständiga loggar här](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})`
            })

      - name: Comment PR
        if: success()
        uses: actions/github-script@v8
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `## 📊 Build Statistik\n\n` +
                    `✅ Build lyckades på Ubuntu!\n\n` +
                    `### Ändringar i denna PR:\n` +
                    `- **Ändrade .cs filer:** ${{ steps.stats.outputs.changed_files }}\n` +
                    `- **Ändrade rader:** ${{ steps.stats.outputs.changed_lines }}\n\n` +
                    `✨ Code analysis: Inga varningar eller fel\n` +
                    `✅ Build komplett - tester körs i separat workflow`
            })

  run-tests:
    needs: build
    uses: ./.github/workflows/test-on-build.yml
    with:
      pr_number: ${{ github.event.pull_request.number }}
      commit_sha: ${{ github.sha }}
