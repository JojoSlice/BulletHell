name: Auto assign author to PR and issue
on:
  issues:
    types: [ opened, reopened ]
  pull_request:
    types: [ opened, reopened ]

permissions:
  issues: write
  pull-requests: write

jobs:
  assign-author:
    runs-on: ubuntu-latest
    steps:
      - name: Assign Author
        uses: toshimaru/auto-author-assign@v2
        with:
          repo-token: ${{ github.token }}
